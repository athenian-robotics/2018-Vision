#include "LineRecog.h"

bool LineRecog::isColor(std::vector<int> in, int primaryIdx, std::vector<float> ratios) {
    return false;
}

std::vector<cv::Point> LineRecog::find_line_contour(cv::Mat frame) {
    return std::vector<cv::Point>();
}
